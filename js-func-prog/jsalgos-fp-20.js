// function sentensify(str) {
//   // Only change code below this line
//   return str.split(/\W/).join(" ");

//   // Only change code above this line
// }
// sentensify("May-the-force-be-with-you");

//reverses a positive number
let blah = function (x) {
  x = "56789";

  let isNegative = false;

  //check if a negative number (x <  0)
  //if negative number
  // - convert to positive number.
  // - set flag to true;
  //  isNegative = true;

  //all your logic which works with positive numbers
  //output = y

  if (isNegative) {
    y = y * -1;
  } else {
    y = y;
  }

  //ternary expression
  isNegative === true ? (y = y * -1) : (y = y); //react

  return y;
};
